* use `make check` to use shellcheck to verify the shell scripts prior to task completion
* any bash shell scripts written should be cross-plaform and run on macos and linux under either gnu or bsd toolsets
